// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://tzejian.github.io/',
  base: '/mac_sys_portfolio_v2/',
  trailingSlash: 'always'
});
